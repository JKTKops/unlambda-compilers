```si``s`k.x`kd`ci

# ``(S1 i)``s(K1 .x)(K1 d)`ci
# ``(S1 i)(S2 (K1 .x) (K1 d)) `ci
# `(S2 i (S2 (K1 .x) (K1 d)))`ci
# ``i`ci`(S2 (K1 .x) (K1 d))`ci
# `<cont>``(K1 .x)`ci`(K1 d)`ci
# `<cont>`.xd
# `<cont>d => x
# ``id`(S2 (K1 .x) (K1 d))`ci
# `d`...
# [promise]